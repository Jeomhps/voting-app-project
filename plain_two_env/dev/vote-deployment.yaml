apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: vote
  name: vote
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vote
  template:
    metadata:
      labels:
        app: vote
    spec:
      containers:
      - image: voting-app/vote
        imagePullPolicy: Never  # force local image to be used
        name: vote
        env:
          - name: ENVIRONMENT
            value: dev  # Variable for "DEV ENV" banner
        ports:
        - containerPort: 80
          name: vote
